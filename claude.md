Petzold style C application
No dependencies so can create a single executable file that can run any Windows PC without needing a bunch of other files
Latest version of C language

The app should read a CSV file
The app should check for headers
The app will map the fields found to the required fields for a valid ICS file
The app should create a valid ICS file for the events listed in the CSV file
There should be the option to export separate ICS files for each event or just one ICS file with all events